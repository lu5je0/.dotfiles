#compdef q-transfer

_q_transfer() {
  _arguments -C -s \
    '(-r --register)'{-r,--register}'[register and authorize with server]:host: ' \
    '(-l --logout)'{-l,--logout}'[logout from server]:host: ' \
    '(-y --yes)'{-y,--yes}'[skip confirmation]' \
    '--gzip[enable gzip compression]' \
    '(-h --help)'{-h,--help}'[show help]' \
    '*:file:_files'
}

_q_transfer "$@"
