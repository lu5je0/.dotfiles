# 输入法环境变量（fcitx5）
# systemd user session 会读取 ~/.config/environment.d/*.conf，Plasma 由 systemd 托管时生效。
# 不设置这些变量时，应用不会与 fcitx5 建立 input context，表现为完全切不到输入法。
GTK_IM_MODULE=fcitx
QT_IM_MODULE=fcitx
XMODIFIERS=@im=fcitx
