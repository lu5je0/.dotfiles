[package]
name = "q-trash"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
percent-encoding = "2"
libc = "0.2"

[profile.release]
strip = true
lto = true
opt-level = "z"
